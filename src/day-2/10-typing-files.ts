import {calculate} from 'external-library';
import classes from 'external.module.css';

calculate(1, 3);

window.playVideo();

const error: ErrorFirst = {name: 'str'};

globalThis.window.playVideo();
